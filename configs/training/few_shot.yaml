epochs: 50
pretrain_epochs: 10
meta_warmup_epochs: 10
batch_size: 16
optimizer: AdamW
lr: 0.0005
weight_decay: 0.01

theta_optim:
  optimizer: AdamW
  lr: 0.0005

phi_optim:
  optimizer: Adam
  lr: 0.00005

psi_optim:
  optimizer: Adam
  lr: 0.000005

lambda_tc: 1.0
lambda_stab: 0.2
lambda_abs: 0.05
