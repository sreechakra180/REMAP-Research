# miniImageNet Few-Shot Benchmark Configuration
dataset:
  name: "mini_imagenet"
  root: "./data/miniimagenet"
  split: "test"

evaluation:
  n_way: 5
  k_shot: 5
  q_query: 15
  n_episodes: 1000

model:
  input_channels: 3
  hidden_dim: 128
